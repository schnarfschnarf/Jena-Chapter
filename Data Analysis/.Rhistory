) +
geom_smooth(
method = "lm"
)
p7
knitr::opts_chunk$set(echo = TRUE)
library(dplyr)
library(readr)
library(readxl)
library(lme4)
library(lme4)
library(ggplot2)
library(patchwork)
library(patchwork)
library(brms)
library(lme4)
library(ggplot2)
library(patchwork)
library(brms)
library(performance)
theme_set(theme_minimal(base_size = 14)+
theme(plot.title = element_text(size=13,hjust=0.5),
axis.text = element_text(size = 7),
axis.text.y = element_text(size = 13),
axis.text.x = element_text(size = 13)))
#load synthesis dataset
synthDat <- read_xlsx("C:/Users/mm68tulo/Desktop/Jena Chapter/Data Analysis/1_Data/synthesized_means_data.xlsx")
synthDat$plotcode <- as.factor(synthDat$plotcode) #change the plotcode to a factor
# add up all the low costs for lower total value
synthDat$costs_total_lo <- synthDat$SOC_costs_lo + synthDat$leach_costs_lo + synthDat$SGM_mean
# add up all the high costs for higher total value
synthDat$costs_total_hi <- synthDat$SOC_costs_hi + synthDat$leach_costs_hi + synthDat$SGM_mean
noMono <- synthDat %>%
filter(sowndiv != 1) #filter out monocultures
# Biomass against species richness
fitBiomassSR <- brm(
mean_biomass ~ log2(sowndiv),
synthDat)
# SGM against species richness
fitSGM <- brm(
SGM_mean ~ log2(sowndiv),
synthDat)
# SOC against species richness
fitSOC <- brm(
mean_org_diff ~ log2(sowndiv),
synthDat)
# Leaching costs against species richness
fitLeach <- brm(
leach_costs_mid ~ log2(sowndiv),
synthDat)
# Total Costs against species richness
fitTotal <- brm(
costs_total_mid ~ log2(sowndiv),
synthDat)
# Biomass against species richness
fitBiomassSR2 <- brm(
mean_biomass ~ log2(sowndiv),
noMono)
# SGM against species richness
fitSGM2 <- brm(
SGM_mean ~ log2(sowndiv),
noMono)
# SOC against species richness
fitSOC2 <- brm(
mean_org_diff ~ log2(sowndiv),
noMono)
# Leaching costs against species richness
fitLeach2 <- brm(
leach_costs_mid ~ log2(sowndiv),
noMono)
# Total Costs against species richness
fitTotal2 <- brm(
costs_total_mid ~ log2(sowndiv),
noMono)
# SGM against species richness
fitSGM3 <- brm(
SGM_mean ~ meanSR,
noMono)
# Total Costs against species richness
fitTotal3 <- brm(
costs_total_mid ~ meanSR,
noMono)
# SGM against species richness
fitSGM4 <- brm(
SGM_mean ~ meanSR,
noMono)
# Total Costs against species richness
fitTotal4 <- brm(
costs_total_mid ~ meanSR,
noMono)
# check output of model
summary(fitTotal4)
# check model assumptions
pp_check(fitTotal4, type="scatter_avg")
pp_check(fitTotal4, ndraws=50)
check_model(fitTotal4, check="linearity")
check_model(fitTotal4, check="homogeneity")
plot(fitTotal4)
# SGM against species richness
fitSGM3 <- brm(
SGM_mean ~ meanSR,
synthDat)
# Total Costs against species richness
fitTotal3 <- brm(
costs_total_mid ~ meanSR,
synthDat)
# check model assumptions
pp_check(fitTotal4, type="scatter_avg")
pp_check(fitTotal4, ndraws=50)
check_model(fitTotal4, check="linearity")
check_model(fitTotal4, check="homogeneity")
# check model assumptions
pp_check(fitTotal2, type="scatter_avg")
pp_check(fitTotal2, ndraws=50)
check_model(fitTotal2, check="linearity")
check_model(fitTotal2, check="homogeneity")
check_model(fitTotal2, check="linearity")
# check model assumptions
pp_check(fitLeach4, type="scatter_avg")
# check model assumptions
pp_check(fitBiomassSR, type="scatter_avg")
# check model assumptions
pp_check(fitSOC2, type="scatter_avg")
pp_check(fitSOC2, ndraws=50)
check_model(fitSOC2, check="linearity")
check_model(fitSOC2, check="homogeneity")
# Total Costs against species richness
fitTotal2 <- brm(
costs_total_mid ~ log2(sowndiv),
noMono, "log")
# Total Costs against species richness
fitTotal2 <- brm(
costs_total_mid ~ log2(sowndiv),
noMono, Gamma(link ="log"))
# Total Costs against species richness
fitTotal2 <- brm(
costs_total_mid ~ log2(sowndiv),
noMono, student())
# check model assumptions
pp_check(fitTotal2, type="scatter_avg")
check_model(fitTotal2, check="linearity")
plot(fitTotal2)
check_model(fitTotal2, check="linearity")
check_model(fitTotal2, check="homogeneity")
# check output of model
summary(fitTotal2)
fitTotal2.1 <- brm(
costs_total_mid ~ log2(sowndiv),
noMono)
loo(fitTotal2, fitTotal2.1) # compare the two models with and without student-t distribution
# check model assumptions
pp_check(fitSGM, type="scatter_avg")
pp_check(fitSGM, ndraws=50)
check_model(fitSGM, check="linearity")
check_model(fitSGM, check="homogeneity")
# check model assumptions
pp_check(fitTotal, type="scatter_avg")
pp_check(fitTotal, ndraws=50)
check_model(fitTotal, check="linearity")
check_model(fitTotal, check="homogeneity")
plot(fitTotal)
# check output of model
summary(fitTotal)
# check model assumptions
pp_check(fitLeach, type="scatter_avg")
pp_check(fitLeach, ndraws=50)
check_model(fitLeach, check="linearity")
check_model(fitLeach, check="homogeneity")
plot(fitLeach)
# check model assumptions
pp_check(fitSOC, type="scatter_avg")
check_model(fitSOC, check="linearity")
check_model(fitSOC, check="linearity")
pp_check(fitSOC, ndraws=50)
check_model(fitSOC, check="homogeneity")
plot(fitSOC)
# check model assumptions
pp_check(fitTotal, type="scatter_avg")
check_model(fitTotal, check="linearity")
# check model assumptions
pp_check(fitTotal2, type="scatter_avg")
pp_check(fitTotal2, ndraws=50)
check_model(fitTotal2, check="linearity")
check_model(fitTotal2, check="homogeneity")
plot(fitTotal2)
fitTotal2.1 <- brm(
costs_total_mid ~ log2(sowndiv),
sigma ~ log2(sowndiv),
noMono)
fitTotal2.1 <- brm(
costs_total_mid ~ log2(sowndiv),
sigma ~ log2(sowndiv),
noMono,
student())
fitTotal2.1 <- brm(
costs_total_mid ~ s(log2(sowndiv)),
noMono)
fitTotal2.1 <- brm(
costs_total_mid ~ s(log2(sowndiv)),
noMono, student())
fitTotal2.1 <- brm(
costs_total_mid ~ s(log2(sowndiv)),
noMono, student())
fitTotal2.1 <- brm(
bf(costs_total_mid ~ log2(sowndiv),
sigma ~ log2(sowndiv)),
noMono)
# check model assumptions
pp_check(fitTotal2.1, type="scatter_avg")
pp_check(fitTotal2.1, ndraws=50)
check_model(fitTotal2.1, check="linearity")
check_model(fitTotal2.1, check="homogeneity")
plot(fitTotal2.1)
loo(fitTotal2, fitTotal2.1) # compare the two models with and without student-t distribution
knitr::opts_chunk$set(echo = TRUE)
library(dplyr)
library(readr)
library(readxl)
library(lme4)
library(lme4)
library(ggplot2)
library(patchwork)
library(brms)
library(ggplot2)
library(patchwork)
library(brms)
library(performance)
theme_set(theme_minimal(base_size = 14)+
theme(plot.title = element_text(size=13,hjust=0.5),
axis.text = element_text(size = 7),
axis.text.y = element_text(size = 13),
axis.text.x = element_text(size = 13)))
#load synthesis dataset
synthDat <- read_xlsx("C:/Users/mm68tulo/Desktop/Jena Chapter/Data Analysis/1_Data/synthesized_means_data.xlsx")
# add up all the low costs for lower total value
synthDat$costs_total_lo <- synthDat$SOC_costs_lo + synthDat$leach_costs_lo + synthDat$SGM_mean
# add up all the high costs for higher total value
synthDat$costs_total_hi <- synthDat$SOC_costs_hi + synthDat$leach_costs_hi + synthDat$SGM_mean
noMono <- synthDat %>%
filter(sowndiv != 1) #filter out monocultures
synthDat$plotcode <- as.factor(synthDat$plotcode) #change the plotcode to a factor
View(noMono)
knitr::opts_chunk$set(echo = TRUE)
library(dplyr)
library(dplyr)
library(readr)
library(dplyr)
library(readr)
library(readxl)
library(lme4)
library(lme4)
library(ggplot2)
library(ggplot2)
library(patchwork)
library(brms)
library(brms)
library(performance)
theme_set(theme_minimal(base_size = 14)+
theme(plot.title = element_text(size=13,hjust=0.5),
axis.text = element_text(size = 7),
axis.text.y = element_text(size = 13),
axis.text.x = element_text(size = 13)))
#load synthesis dataset
synthDat <- read_xlsx("C:/Users/mm68tulo/Desktop/Jena Chapter/Data Analysis/1_Data/synthesized_means_data.xlsx")
knitr::opts_chunk$set(echo = TRUE)
library(dplyr)
library(readr)
library(readxl)
library(lme4)
library(lme4)
library(ggplot2)
library(patchwork)
library(ggplot2)
library(patchwork)
library(brms)
library(lme4)
library(ggplot2)
library(patchwork)
library(brms)
library(performance)
theme_set(theme_minimal(base_size = 14)+
theme(plot.title = element_text(size=13,hjust=0.5),
axis.text = element_text(size = 7),
axis.text.y = element_text(size = 13),
axis.text.x = element_text(size = 13)))
#load synthesis dataset
synthDat <- read_xlsx("C:/Users/mm68tulo/Desktop/Jena Chapter/Data Analysis/1_Data/synthesized_means_data.xlsx")
synthDat$plotcode <- as.factor(synthDat$plotcode) #change the plotcode to a factor
# add up all the low costs for lower total value
synthDat$costs_total_lo <- synthDat$SOC_costs_lo + synthDat$leach_costs_lo + synthDat$SGM_mean
# add up all the high costs for higher total value
synthDat$costs_total_hi <- synthDat$SOC_costs_hi + synthDat$leach_costs_hi + synthDat$SGM_mean
noMono <- synthDat %>%
filter(sowndiv != 1) #filter out monocultures
View(synthDat)
noMono <- synthDat %>%
filter(sowndiv > 1 & sowndiv < 60) #filter out monocultures
#check distribution of mid total costs across sowndiv
p5 <- ggplot2::ggplot(noMono,
aes(x = meanSR, y = costs_total_mid)
) +
geom_point(
color = "#28a87d",
alpha = .5,
stroke = 1,
size = 4
) +
labs(
x = "",
y = ""
) +
geom_smooth(
method = "lm"
)
p5
#check distribution of mid total costs across sowndiv
p5 <- ggplot2::ggplot(noMono,
aes(x = meanSR, y = costs_total_mid)
) +
geom_point(
color = "#28a87d",
alpha = .5,
stroke = 1,
size = 4
) +
labs(
x = "",
y = ""
) +
geom_smooth(
method = "lm",
se = FALSE,
color = "blue"
) +
stat_poly_eq(
aes(label = paste(..eq.label.., ..rr.label.., sep = "~~~")),
formula = y ~ x,
parse = TRUE,
size = 5
)
install.packages("ggpmisc")
library(ggpmisc)
#check distribution of mid total costs across sowndiv
p5 <- ggplot2::ggplot(noMono,
aes(x = meanSR, y = costs_total_mid)
) +
geom_point(
color = "#28a87d",
alpha = .5,
stroke = 1,
size = 4
) +
labs(
x = "",
y = ""
) +
geom_smooth(
method = "lm",
se = FALSE,
color = "blue"
) +
stat_poly_eq(
aes(label = paste(..eq.label.., ..rr.label.., sep = "~~~")),
formula = y ~ x,
parse = TRUE,
size = 5
)
p5
# Total Costs against species richness
fitTotal4 <- brm(
costs_total_mid ~ meanSR,
noMono)
# check model assumptions
pp_check(fitTotal4, type="scatter_avg")
pp_check(fitTotal4, ndraws=50)
check_model(fitTotal4, check="linearity")
check_model(fitTotal4, check="homogeneity")
plot(fitTotal4)
# check output of model
summary(fitTotal4)
View(noMono)
hist(synthDat$costs_total_mid, breaks = 20, main = "Histogram of Total Costs", xlab = "Total Costs (€/ha/a)", col = "lightblue")
hist(synthDat$costs_total_mid, breaks = 30, main = "Histogram of Total Costs", xlab = "Total Costs (€/ha/a)", col = "lightblue")
hist(synthDat$costs_total_mid, breaks = 40, main = "Histogram of Total Costs", xlab = "Total Costs (€/ha/a)", col = "lightblue")
hist(synthDat$costs_total_mid, breaks = 10, main = "Histogram of Total Costs", xlab = "Total Costs (€/ha/a)", col = "lightblue")
# Total Costs against species richness
fitTotal2 <- brm(
costs_total_mid ~ log2(sowndiv),
noMono, student())
View(noMono)
#check distribution of mid total costs across sown diversity
pCostsSown <- ggplot2::ggplot(noMono,
aes(x = sowndiv, y = costs_total_mid)
) +
geom_point(
color = "#28a87d",
alpha = .5,
stroke = 1,
size = 4
) +
labs(
x = "",
y = ""
) +
geom_smooth(
method = "lm",
se = FALSE,
color = "blue"
) +
stat_poly_eq(
aes(label = paste(..eq.label.., ..rr.label.., sep = "~~~")),
formula = y ~ x,
parse = TRUE,
size = 5
)
pCostsSown
#check distribution of mid total costs across realized diversity
pCostsReal <- ggplot2::ggplot(noMono,
aes(x = meanSR, y = costs_total_mid)
) +
geom_point(
color = "#28a87d",
alpha = .5,
stroke = 1,
size = 4
) +
labs(
x = "",
y = ""
) +
geom_smooth(
method = "lm",
se = FALSE,
color = "blue"
) +
stat_poly_eq(
aes(label = paste(..eq.label.., ..rr.label.., sep = "~~~")),
formula = y ~ x,
parse = TRUE,
size = 5
)
pCostsReal
#check distribution of mid total costs across sown diversity
pCostsSown <- ggplot2::ggplot(noMono,
aes(x = log2(sowndiv), y = costs_total_mid)
) +
geom_point(
color = "#28a87d",
alpha = .5,
stroke = 1,
size = 4
) +
labs(
x = "",
y = ""
) +
geom_smooth(
method = "lm",
se = FALSE,
color = "blue"
) +
stat_poly_eq(
aes(label = paste(..eq.label.., ..rr.label.., sep = "~~~")),
formula = y ~ x,
parse = TRUE,
size = 5
)
pCostsSown
2^212
# check model assumptions
pp_check(fitTotal2, type="scatter_avg")
pp_check(fitTotal2, ndraws=50)
check_model(fitTotal2, check="linearity")
# Total Costs against species richness
fitTotal2 <- brm(
costs_total_mid ~ log2(sowndiv),
noMono)
# check model assumptions
pp_check(fitTotal2, type="scatter_avg")
pp_check(fitTotal2, ndraws=50)
check_model(fitTotal2, check="linearity")
check_model(fitTotal2, check="homogeneity")
plot(fitTotal2)
# check output of model
summary(fitTotal2)
loo(fitTotal2, fitTotal4) # compare the two models with and without student-t distribution
# check model assumptions
pp_check(fitTotal2, type="scatter_avg")
check_model(fitTotal2.1, check="linearity")
check_model(fitTotal2, check="linearity")
pp_check(fitTotal2, ndraws=50)
# check model assumptions
pp_check(fitTotal2, type="scatter_avg")
pp_check(fitTotal2, ndraws=50)
check_model(fitTotal2, check="linearity")
check_model(fitTotal2, check="homogeneity")
check_model(fitTotal2, check="homogeneity")
check_model(fitTotal2, check="linearity")
check_model(fitTotal2, check="homogeneity")
plot(fitTotal2)
check_model(fitTotal2, check="linearity")
# check output of model
summary(fitTotal4)
plot(fitTotal4)
# check model assumptions
pp_check(fitTotal4, type="scatter_avg")
pp_check(fitTotal4, ndraws=50)
check_model(fitTotal4, check="linearity")
check_model(fitTotal4, check="homogeneity")
check_model(fitTotal4, check="linearity")
plot(fitTotal4)
pp_check(fitTotal4, ndraws=50)
